# Core runtime dependencies for WasmMend.
# Versions reflect what was used in the paper's evaluation; newer
# point releases of each library are expected to work.

# libclang Python bindings — used for AST traversal in the
# instrumentation and type-extraction passes. The matching native
# libclang shared library must also be installed on the system
# (e.g. `apt install libclang-15-dev` on Debian/Ubuntu).
clang>=15.0.0

# Google Gemini SDK (the modern `from google import genai` API).
# Used by the GeminiAgent backend.
google-genai>=1.0.0

# OpenAI-compatible client.
# Used by the ChatGPTAgent (OpenAI), DeepSeekAgent (DeepSeek), and
# QwenAgent (DashScope) backends — DeepSeek and Qwen both expose
# OpenAI-compatible endpoints.
openai>=1.0.0

# Progress bars during parallel instrumentation.
tqdm>=4.60.0

# Optional: only required if you pass --collect to
# divergence_guided_repair.py to append run summaries to an
# Excel spreadsheet.
openpyxl>=3.0.0
